import React from 'react';
import zero from './../../assets/zero.svg';

const Zero = () => {
    return (
        <img style={{width: "100%", height: "100%"}} src={zero} alt=""/>
    );
};

export default Zero;